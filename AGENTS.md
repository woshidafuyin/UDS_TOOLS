## Git提交规则

- 每次代码修改完成并且测试通过后，必须创建一次本地Git commit。
- 每个独立任务只创建一个commit。
- 只提交本次任务修改的文件，不得提交工作区原有的无关改动。
- 测试失败时不得commit，应先报告问题。
- commit message必须清楚说明修改目的，格式如下：

  type(scope): 简短说明

  - 主要改动1
  - 主要改动2
  - 测试结果

- type使用：fix、feat、refactor、test、docs、build、style。
- 完成后报告commit ID、提交文件、测试结果和未提交内容。
- 只创建本地commit，不执行git push。
## 构建与dist规则

- 不要长期创建或保留dist-日期、dist-validation等旁路交付目录。
- 修改代码后先在build或临时目录完成编译和测试。
- 测试全部通过后，自动更新正式目录：
  `D:/project/UDS_tools/dist`
- 测试失败时不得覆盖正式dist。
- 更新dist前确认CH_FLASH_tools.exe没有正在运行。
- 更新完成后自动删除临时构建或候选交付目录。
- 不需要用户手动合并候选目录与正式dist。
- 最后创建本地Git commit，不执行git push。
- 只有实际修改了代码或配置文件时才创建commit；只读分析不创建commit。
  ## 架构与可维护性原则

每次修改代码时，都必须从整个项目的角度评估改动，而不是只解决当前局部问题。目标是持续提升项目的可维护性、可扩展性和可测试性。

必须遵守以下原则：

1. 保持低耦合、高内聚：
   - 单个模块、类或函数应承担清晰且相对单一的职责。
   - UI、业务流程、协议逻辑、硬件访问、配置管理、日志与报告等职责应合理分层。
   - 避免模块之间直接依赖不必要的内部实现。
   - 跨模块交互优先通过明确的接口、数据结构或事件完成。
   - 避免新增全局状态、隐式依赖、循环依赖和跨层调用。

2. 修改前必须分析影响范围：
   - 当前修改会影响哪些模块、接口、状态和业务流程。
   - 是否会导致职责混乱、重复实现或模块耦合增加。
   - 是否破坏已有接口、配置格式、刷写流程、硬件行为或测试用例。
   - 是否存在更合适的复用位置或抽象边界。

3. 修改时优先采用最小且完整的方案：
   - 不为解决局部问题引入大范围无关重构。
   - 不为了“低耦合”而机械地增加过多接口、包装层、类或设计模式。
   - 如果现有设计足以清晰完成需求，应优先保持简单。
   - 如确实需要重构，应说明必要性、影响范围、迁移方式和回归风险。

4. 修改完成后必须进行架构风险检查，并在结果中说明：
   - 本次改动新增或改变了哪些依赖。
   - 是否降低或增加了模块耦合。
   - 相关职责是否仍然集中在正确模块中。
   - 是否出现重复代码、跨层调用、隐式状态或兼容性风险。
   - 需要执行哪些回归测试。
   - 是否存在暂未处理的技术债务。

5. 不得仅以“代码可以运行”作为完成标准。还必须确保：
   - 模块职责清晰。
   - 依赖方向合理。
   - 命名能够表达业务含义。
   - 异常和边界情况得到处理。
   - 修改具有必要的测试或验证依据。
   - 不破坏现有功能和对外行为。