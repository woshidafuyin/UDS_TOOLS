UDS 通用刷写工具 Change List
========================================

发布日期：2026-08-21

文档职责：
- 本文件只记录当前发布包相对 8.19 正式包的变化。
- 当前产品的完整功能、项目、模式和使用边界见包内 README.md。
- 离线测试、硬件连通、真实 ECU 刷写是不同证据层级。

一、比较基线
----------------------------------------

上一正式包：UDS_Tool_Release_20260819_ARC_4A7E87EC.zip
- 文件大小：81,568,583 bytes
- SHA-256：D8DA98EC15338F86ED2B6F33EC62EC824B0C7881E54539AC35EA74BBC4605E90

上一正式主程序：uds_tool_qt.exe
- 文件大小：1,551,360 bytes
- SHA-256：4A7E87ECC57A041C9A188B95E0D1269A2D77367B82CC3E7C050CFBA23FBACECB

本次正式主程序：UDS_Tool.exe
- 文件大小：1,520,128 bytes
- SHA-256：0708C1B77353239B7C2E27B42F97724A495E0D86523CE68DF208E8BB4D9B5E8A

比较方法：
- 使用相对路径和 SHA-256 比较文件，不用文件名、大小或时间戳代替内容判断。
- 功能变化同时核对源码实现和离线测试。
- 源码变化不自动等同于硬件或 ECU 台架验收。

二、功能变化
----------------------------------------

1. 总线监听 Trace 和过滤

- 工具启动后自动被动监听当前刷写通道，无需手工点击开始。
- 完整 Trace 持续写盘，不受 UI 最近 10,000 帧缓存限制。
- 运行中使用 .partial，正常结束形成 ASC，异常遗留文件可在下次启动时恢复。
- 导出完整磁盘 Trace，而不是只导出当前表格缓存。
- 支持诊断 ID 精确值、范围、掩码和排除过滤。
- 增加当前项目诊断 ID、功能寻址、物理寻址和周期帧快捷过滤。
- 区分总接收、当前显示和内存淘汰帧数。
- 修正 NRC 识别，避免把 Seed、签名或刷写数据中的 7F 误判为负响应。

2. 监听完整跟随 CAN 硬件上下文

- 监听上下文包含硬件后端、物理通道、仲裁/数据波特率和 CAN/CAN FD。
- 监听页显示 Vector、ZLG、TOSUN、Kvaser 等当前后端和通道。
- 切换项目、目标、后端或通道后自动释放旧监听并在新上下文恢复。
- 探测、读取和刷写前检查监听上下文与当前配置一致；无法对齐时失败关闭。
- 被动监听不发送任何 CAN/UDS 报文。

3. ZLG 零发送后的共享通道恢复

- 独立识别“适配器确认发送 0 帧”的错误，并保留底层通道错误信息。
- 单帧明确发送 0 帧时关闭并重建共享通道，然后安全重试一次。
- 只有确认发送成功后才发布 TX 记录。
- 多帧或部分发送状态不确定时不整批自动重放，避免重复 UDS 服务或刷写数据。

4. 刷写资格和完整配置审计

- 记录最近一次“能否刷写”的结果、完成时间和配置指纹。
- 探测后改变配置会标记 STALE_CONFIG，不把旧结果当成当前配置证明。
- 记录硬件后端、通道、Tx/Rx ID、仲裁/数据波特率、CAN FD、Padding 和入口模式。
- 记录 Driver、校验、APP、CAL、SeedKey 等文件路径、存在性和大小。
- 审计信息同时进入运行日志和 HTML 报告。

5. 目标身份审计

- 记录厂商、项目、设备显示名、Profile ID、Target ID 和 Flow ID。
- 记录入口模式和刷写重复次数。
- 目标显示名与 Profile、Target、Tx/Rx ID 组合记录，避免仅凭中文名称判断 ECU。

6. 执行日志持续尾随

- 按 End 后跳到当前末尾并进入持续尾随模式。
- 后续日志到达后继续滚动到最新末尾。
- 按 Home 或向上滚动阅读历史时暂停尾随。
- 再次按 End 或滚到底部后恢复尾随。

7. ARC331 入口和中断状态处理

- APP“能否刷写”在 10 03 后继续检查 31 01 02 03。
- NRC 0x31 会提示 ECU 可能处于擦除中断后的 Boot/SBL 恢复态，并禁止把它误判为普通 APP 可刷写。
- Boot 恢复引擎保留为内部受控能力，正式发布包不在模式下拉框中显示该入口。
- 正式界面保留 APP 和 FT；恢复操作必须使用单独、显式授权的受控构建。
- 日志和报告按实际 Target 显示左/右雷达身份。

三、正式交付包清理
----------------------------------------

本次将发布目录收敛为运行时交付物，删除以下内部或非运行文件：

- 历史 logs、Configuration 和 .partial Trace。
- validation 台架验收记录和硬件序列号等内部证据。
- can_hardware_probe.exe、kvaser_preflight.ps1 等台架预检工具。
- Kvaser 台架清单、源码级 Flow Parity 和过时楚能专版 README。
- ZLG/TOSUN SDK 的 .h 头文件和 .lib 导入库；运行 DLL、配置和许可证保留。
- resources 下未被 Profile 使用的 Reference 参考工程、历史报告和 PLS 备份。
- 吉利 P416 的 *_reconstructed.vbf 解析验证样本；正式 Profile 默认 VBF 保留。
- SOURCE_MANIFEST、RESOURCE_MANIFEST、PROVENANCE、BASELINE 等内部来源记录。

正式包继续保留：

- UDS_Tool.exe 和 32 位 SeedKey 代理 keygen_broker.exe。
- Qt、VC Runtime、OpenGL 软件回退等运行依赖。
- 18 个正式 Profile。
- Profile 明确引用的 Driver、APP、CAL、CBF、VBF、校验文件和 SeedKey DLL。
- Vector、ZLG、TOSUN、Kvaser 运行所需 DLL、配置及许可证。
- 用户 README 和本 Change List。

清理后的文件清单：
- 上一正式包实际文件：369 个。
- 本次正式包实际文件：310 个。
- 与上一正式包内容完全相同：307 个。
- 同路径内容变化：README.md。
- 上一正式包独有：旧主程序及 60 个已清理的内部/非运行文件。
- 本次正式包独有：UDS_Tool.exe、CHANGE_LIST.txt。

四、未变化的产品范围
----------------------------------------

- 18 个项目 Profile 的配置内容未因交付清理而修改。
- 15 个已注册 Workflow ID 未因交付清理而减少。
- Profile 明确引用的正式刷写输入和 SeedKey DLL 均保留。
- Vector、ZLG、TOSUN、Kvaser 四类硬件后端仍在主程序中启用。
- 本次清理不删除源码仓库中的测试、validation、Reference 和来源记录，只是不交付给最终用户。

五、验证状态和边界
----------------------------------------

当前源码/构建离线 CTest：8/8 PASS
- core_tests
- geely_p416_tests
- can_adapter_tests
- vendor_api_boundaries
- vendor_reference_manifest
- app_state_tests
- qt_probe_bridge_tests
- qt_main_window_tests

交付门禁：
- dist 与 ZIP 必须逐相对路径、逐 SHA-256 完全一致。
- 包内不得包含 logs、Configuration、validation、tools、.partial、开发头文件/导入库、Reference 或内部来源记录。
- 所有 Profile 非空文件引用必须在 dist 中存在。
- 最终 ZIP 的 SHA-256 记录在同名 .sha256.txt 文件中。

边界说明：
- 8/8 PASS 是源码、适配器、状态机和 Qt UI 的离线/构建回归证据。
- 发布清理后的启动和离线测试不能替代所有硬件后端与 ECU 的实车验收。
- 某项目或设备的 APP PASS 不自动证明 FT、CAL、另一设备或相似项目通过。
- ARC331 Boot 恢复能力不在普通发布包 UI 中开放。
